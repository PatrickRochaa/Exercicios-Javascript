function calcular(){
    var num = Number(window.prompt("Informe um numero inteiro"))
   var antes = num-1
   var depois = num+1
    window.alert(`O antecessor de ${num}, é o número ${antes}
    O sucessor de ${num}, é o número ${depois}`)
}